public class SimpleArray {
    public static void main(String[] args) {
        int[] numbers = {1, 2, 3, 4, 5};

        for (int i = 0; i < numbers.length; i++) {
            System.out.println("Элемент массива с индексом " + i + " равен " + numbers[i]);
        }
    }
}